module Typeclasses.Apply exposing (..)

-- type alias Apply a b fa fb =
--     { map : (a -> b) -> fa -> fb
--     , ap : fa (a -> b) -> fa -> fb
--     }


-- apList : Apply a b (List a) (List b)
-- apList = 
--   { map = List.map
--   , ap = 2
--   }